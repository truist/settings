#!/bin/sh

set -eu

if [ "$#" -ne 1 ] || [ -z "$1" ]; then
        echo "args: <branch name>" >&2
        exit 1
fi


# device ID (0-3) to use
# megabytes to generate

# path to secure dir
# image file name
# vnd device
# cypher / bits
# cgd device
# mount point

# set all the permissions!

