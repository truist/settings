# Use this file like so:
#[include]
#	path = code/settings/gitconfig

[user]
	name = Nathan Arthur
#	email = SET ONE IN YOUR LOCAL FILE

[alias]
	cleanall = clean -d -x -f
	graph = log --graph --all --pretty=format':%C(yellow)%h%Cred%d%Creset %s %Cgreen %an, %ar%Creset'
	master-to-new-branch = "!master-to-new-branch"
	search = "!f() { git log -p -G\"$1\" --diff-filter=M ; }; f"
	st = status -s
	undo-commit = reset --soft HEAD^
	wdiff = diff --color-words
	wshow = show --color-words

[branch]
	autosetuprebase = always

[color]
	ui = true
[color "diff"]
	meta = yellow

[core]
	excludesfile = ~/.gitignore
	pager = less -i

#[credential]
#	helper = osxkeychain

[merge]
	tool = vimdiff

[pull]
	rebase = true

[push]
	default = upstream
